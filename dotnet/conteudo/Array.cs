namespace Array;

public class Program {
    public static void Main1() {
        // int[] numbers = new int[5];

        // numbers[0] = 1;

        // int[] anotherNumbers = numbers;

        // anotherNumbers[1] = 2;

        // foreach (var item in numbers) {
        //     Console.WriteLine(item);
        // }

        // int[] diffWayToCreateArrays = { 1, 2, 3, 4, 5 };

        int[,] numbers = new int[5, 2];
        
    
    }
}